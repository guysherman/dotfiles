# Setting up github with multiple accounts via ssh

Run the following to create keys:

```
ssh-keygen -t ed25519 -C "email@address.com" -f ~/.ssh/id_ed25519_accountname

```

Take a look at [this](https://gist.github.com/oanhnn/80a89405ab9023894df7)
